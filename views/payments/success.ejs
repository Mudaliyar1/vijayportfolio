<div class="container mx-auto px-4 py-8 max-w-4xl">
  <div class="bg-dark-100 rounded-lg shadow-lg overflow-hidden mb-8 border border-gray-800">
    <div class="bg-gradient-to-r from-green-600 to-green-500 p-6 text-white text-center">
      <svg class="h-16 w-16 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
      </svg>
      <h1 class="text-3xl font-bold">Payment Successful!</h1>
      <p class="mt-2">Your payment has been processed successfully.</p>
    </div>

    <div class="p-6 text-white">
      <h2 class="text-2xl font-bold mb-4 text-white">Receipt</h2>

      <div class="border border-gray-700 rounded-lg overflow-hidden">
        <table class="w-full text-white">
          <tbody>
            <tr class="border-b border-gray-700">
              <td class="px-4 py-3 font-semibold bg-dark-200 text-blue-300">Payment ID</td>
              <td class="px-4 py-3 font-mono text-sm">
                <% if (payment.razorpayPaymentId) { %>
                  <%= payment.razorpayPaymentId %>
                <% } else { %>
                  <span class="text-gray-400">Not available</span>
                <% } %>
              </td>
            </tr>
            <tr class="border-b border-gray-700">
              <td class="px-4 py-3 font-semibold bg-dark-200 text-blue-300">Order ID</td>
              <td class="px-4 py-3 font-mono text-sm"><%= payment.razorpayOrderId %></td>
            </tr>
            <tr class="border-b border-gray-700">
              <td class="px-4 py-3 font-semibold bg-dark-200 text-blue-300">Package</td>
              <td class="px-4 py-3"><%= payment.package.name %></td>
            </tr>
            <% if (payment.notes && payment.notes.originalPrice && payment.notes.discountAmount) { %>
            <tr class="border-b border-gray-700">
              <td class="px-4 py-3 font-semibold bg-dark-200 text-blue-300">Original Price</td>
              <td class="px-4 py-3 line-through text-gray-400">₹<%= payment.notes.originalPrice %></td>
            </tr>
            <tr class="border-b border-gray-700 bg-green-900/30">
              <td class="px-4 py-3 font-semibold bg-dark-200 text-green-300">Discount</td>
              <td class="px-4 py-3 text-green-400 font-bold">- ₹<%= payment.notes.discountAmount %></td>
            </tr>
            <tr class="border-b border-gray-700 bg-green-900/50">
              <td class="px-4 py-3 font-semibold bg-dark-200 text-green-300">Final Amount</td>
              <td class="px-4 py-3 font-bold text-green-300">₹<%= payment.amount %></td>
            </tr>
            <% } else { %>
            <tr class="border-b border-gray-700">
              <td class="px-4 py-3 font-semibold bg-dark-200 text-blue-300">Amount</td>
              <td class="px-4 py-3 font-bold">₹<%= payment.amount %></td>
            </tr>
            <% } %>
            <tr class="border-b border-gray-700">
              <td class="px-4 py-3 font-semibold bg-dark-200 text-blue-300">Date</td>
              <td class="px-4 py-3"><%= new Date(payment.createdAt).toLocaleString('en-IN', { timeZone: 'Asia/Kolkata' }) %></td>
            </tr>
            <tr class="border-b border-gray-700">
              <td class="px-4 py-3 font-semibold bg-dark-200 text-blue-300">Status</td>
              <td class="px-4 py-3">
                <span class="inline-block px-2 py-1 bg-green-900 text-green-300 rounded">
                  <%= payment.status.toUpperCase() %>
                </span>
              </td>
            </tr>
          </tbody>
        </table>
      </div>

      <div class="mt-8 text-center">
        <p class="mb-4 text-gray-300">You can now create your website using our AI-powered website builder.</p>
        <div class="mb-6">
          <a href="/payment/download/<%= payment._id %>" class="inline-flex items-center bg-gradient-to-r from-green-600 to-green-700 hover:from-green-700 hover:to-green-800 text-white font-bold py-2 px-6 rounded-lg shadow-lg transition-all duration-300 transform hover:-translate-y-1">
            <i class="fas fa-download mr-2"></i> Download Receipt
          </a>
        </div>
        <div class="flex justify-center space-x-4">
          <a href="/create-website" class="bg-gradient-to-r from-blue-600 to-indigo-600 hover:from-blue-700 hover:to-indigo-700 text-white font-bold py-2 px-6 rounded-lg shadow-lg transition-all duration-300 transform hover:-translate-y-1">
            Create Website
          </a>
          <a href="/dashboard/websites" class="bg-gradient-to-r from-gray-700 to-gray-800 hover:from-gray-600 hover:to-gray-700 text-white font-bold py-2 px-6 rounded-lg shadow-lg transition-all duration-300 transform hover:-translate-y-1">
            Go to Dashboard
          </a>
        </div>
      </div>
    </div>
  </div>
</div>
