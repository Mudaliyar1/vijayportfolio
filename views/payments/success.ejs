<div class="container mx-auto px-4 py-8 max-w-4xl">
  <div class="bg-white rounded-lg shadow-lg overflow-hidden mb-8">
    <div class="bg-green-500 p-6 text-white text-center">
      <svg class="h-16 w-16 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
      </svg>
      <h1 class="text-3xl font-bold">Payment Successful!</h1>
      <p class="mt-2">Your payment has been processed successfully.</p>
    </div>
    
    <div class="p-6">
      <h2 class="text-2xl font-bold mb-4">Receipt</h2>
      
      <div class="border rounded-lg overflow-hidden">
        <table class="w-full">
          <tbody>
            <tr class="border-b">
              <td class="px-4 py-3 font-semibold bg-gray-50">Payment ID</td>
              <td class="px-4 py-3 font-mono text-sm"><%= payment.razorpayPaymentId %></td>
            </tr>
            <tr class="border-b">
              <td class="px-4 py-3 font-semibold bg-gray-50">Order ID</td>
              <td class="px-4 py-3 font-mono text-sm"><%= payment.razorpayOrderId %></td>
            </tr>
            <tr class="border-b">
              <td class="px-4 py-3 font-semibold bg-gray-50">Package</td>
              <td class="px-4 py-3"><%= payment.package.name %></td>
            </tr>
            <tr class="border-b">
              <td class="px-4 py-3 font-semibold bg-gray-50">Amount</td>
              <td class="px-4 py-3 font-bold">₹<%= payment.amount %></td>
            </tr>
            <tr class="border-b">
              <td class="px-4 py-3 font-semibold bg-gray-50">Date</td>
              <td class="px-4 py-3"><%= new Date(payment.createdAt).toLocaleString() %></td>
            </tr>
            <tr>
              <td class="px-4 py-3 font-semibold bg-gray-50">Status</td>
              <td class="px-4 py-3">
                <span class="inline-block px-2 py-1 bg-green-100 text-green-800 rounded">
                  <%= payment.status.toUpperCase() %>
                </span>
              </td>
            </tr>
          </tbody>
        </table>
      </div>
      
      <div class="mt-8 text-center">
        <p class="mb-4">You can now create your website using our AI-powered website builder.</p>
        <div class="flex justify-center space-x-4">
          <a href="/create-website" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-6 rounded transition-colors duration-300">
            Create Website
          </a>
          <a href="/dashboard/websites" class="bg-gray-600 hover:bg-gray-700 text-white font-bold py-2 px-6 rounded transition-colors duration-300">
            Go to Dashboard
          </a>
        </div>
      </div>
    </div>
  </div>
</div>
