<div class="container mx-auto px-4 py-8 max-w-4xl">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl font-bold text-white">Add New Page to <%= website.title %></h1>
    <a href="/dashboard/websites/<%= website._id %>" class="text-blue-600 hover:text-blue-800">
      &larr; Back to Website
    </a>
  </div>

  <% if(error_msg != ''){ %>
    <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6" role="alert">
      <p><%= error_msg %></p>
    </div>
  <% } %>

  <% if(success_msg != ''){ %>
    <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mb-6" role="alert">
      <p><%= success_msg %></p>
    </div>
  <% } %>

  <div class="bg-gray-800 rounded-lg shadow-md p-6 text-white">
    <form action="/dashboard/websites/<%= website._id %>/pages/add" method="POST">
      <div class="mb-4">
        <label for="title" class="block text-white font-bold mb-2">Page Title</label>
        <input type="text" id="title" name="title" class="w-full px-3 py-2 border border-gray-700 bg-gray-700 text-white rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required>
        <p class="text-gray-400 text-sm mt-1">This will be displayed in the navigation menu and as the page heading.</p>
      </div>

      <div class="mb-6">
        <label for="content" class="block text-white font-bold mb-2">Page Content</label>
        <textarea id="content" name="content" rows="15" class="w-full px-3 py-2 border border-gray-700 bg-gray-700 text-white rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"></textarea>
        <p class="text-gray-400 text-sm mt-1">You can use HTML to format your content. Leave blank to use a default template.</p>
      </div>

      <div class="flex justify-between">
        <div>
          <button type="submit" name="stay_on_page" value="true" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mr-2">
            Save & Edit
          </button>
          <button type="submit" name="stay_on_page" value="false" class="bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-4 rounded">
            Save & Return
          </button>
        </div>
        <a href="/dashboard/websites/<%= website._id %>" class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded">
          Cancel
        </a>
      </div>
    </form>
  </div>

  <div class="mt-8 bg-gray-800 p-4 rounded-lg">
    <h2 class="text-lg font-bold mb-2 text-white">Tips for Creating Pages</h2>
    <ul class="list-disc list-inside text-gray-300">
      <li>Keep page titles short and descriptive</li>
      <li>Use HTML to format your content (headings, paragraphs, lists, etc.)</li>
      <li>You can add images using the &lt;img&gt; tag</li>
      <li>Your website can have up to <%= website.package.maxPages %> pages</li>
      <li>You currently have <%= website.pages.length %> of <%= website.package.maxPages %> pages</li>
    </ul>
  </div>
</div>
