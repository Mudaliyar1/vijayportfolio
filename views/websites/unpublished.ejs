<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= website.title %> - Currently Unpublished</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      font-family: 'Poppins', sans-serif;
      color: #333;
      height: 100vh;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0;
      padding: 0;
    }
    .container {
      width: 90%;
      max-width: 800px;
      margin: 0 auto;
      padding: 1rem;
      height: 100%;
      max-height: 100vh;
      overflow-y: auto;
    }

    @media (max-width: 640px) {
      .container {
        width: 95%;
        padding: 0.5rem;
      }
    }
    .card {
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
      overflow: hidden;
      backdrop-filter: blur(10px);
      background: rgba(255, 255, 255, 0.95);
      border: 1px solid rgba(255, 255, 255, 0.2);
      max-height: calc(100vh - 4rem);
      display: flex;
      flex-direction: column;
    }
    .card-header {
      background: linear-gradient(to right, #4f46e5, #7c3aed);
      padding: 2rem 1rem;
      color: white;
      position: relative;
      overflow: hidden;
    }

    @media (max-width: 640px) {
      .card-header {
        padding: 1.5rem 1rem;
      }
      .card-header h1 {
        font-size: 1.5rem;
      }
    }
    .card-header::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 60%);
      transform: rotate(30deg);
    }
    .logo {
      width: 90px;
      height: 90px;
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.5rem;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
      border: 4px solid rgba(255, 255, 255, 0.3);
    }

    @media (max-width: 640px) {
      .logo {
        width: 70px;
        height: 70px;
        margin: 0 auto 1rem;
      }
      .logo i {
        font-size: 2rem;
      }
    }
    .logo i {
      font-size: 2.5rem;
      color: white;
    }
    .btn {
      transition: all 0.3s ease;
      transform: translateY(0);
    }
    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 7px 14px rgba(0, 0, 0, 0.1), 0 3px 6px rgba(0, 0, 0, 0.1);
    }
    .alert {
      border-radius: 12px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    }
    /* Ensure no external styles affect this page */
    #unpublished-page-container * {
      font-family: 'Poppins', sans-serif !important;
    }
  </style>
  <!-- Prevent any external scripts from affecting this page -->
  <script>
    // Isolate this page from any parent frames
    if (window.top !== window.self) {
      window.top.location.href = window.self.location.href;
    }
  </script>
</head>
<body>
  <div id="unpublished-page-container" class="container">
    <div class="card">
      <div class="card-header text-center">
        <div class="logo">
          <i class="fas fa-globe"></i>
        </div>
        <h1 class="text-3xl font-bold mb-2"><%= website.title %></h1>
        <p class="text-white text-opacity-80">This website is currently unavailable</p>
      </div>

      <div class="p-8 text-center overflow-y-auto flex-grow" style="scrollbar-width: thin;">
        <div class="max-w-md mx-auto">
        <% if (typeof packageDeleted !== 'undefined' && packageDeleted) { %>
        <div class="alert bg-red-50 border-l-4 border-red-400 p-4 mb-6 text-left">
          <div class="flex">
            <div class="flex-shrink-0">
              <i class="fas fa-exclamation-circle text-red-500"></i>
            </div>
            <div class="ml-3">
              <p class="text-sm text-red-700">
                <span class="font-medium">This website's package has been discontinued</span>
              </p>
            </div>
          </div>
        </div>

        <p class="text-gray-600 mb-8 max-w-md mx-auto text-justify">
          The website you're trying to access is unavailable because its package has been discontinued by the administrator. The website owner needs to select a new package before this website can be accessed again.
        </p>
        <% } else { %>
        <div class="alert bg-yellow-50 border-l-4 border-yellow-400 p-4 mb-6 text-left">
          <div class="flex">
            <div class="flex-shrink-0">
              <i class="fas fa-exclamation-triangle text-yellow-400"></i>
            </div>
            <div class="ml-3">
              <p class="text-sm text-yellow-700">
                <span class="font-medium">This website has been temporarily unpublished by the administrator</span>
              </p>
            </div>
          </div>
        </div>

        <p class="text-gray-600 mb-8 max-w-md mx-auto text-justify">
          The website you're trying to access is currently not available. Please contact the website owner for more information or check back later.
        </p>
        <% } %>

        <div class="bg-gray-50 p-6 rounded-xl mb-6 mx-auto text-left w-full sm:w-auto">
          <h3 class="text-lg font-semibold mb-3 text-gray-700">Website Information</h3>
          <div class="space-y-2 text-sm text-gray-600">
            <p><span class="font-medium">Website Name:</span> <%= website.title %></p>
            <p><span class="font-medium">Owner:</span> <%= user.name || user.username %></p>
            <% if (user.email) { %>
              <p><span class="font-medium">Contact:</span> <%= user.email %></p>
            <% } %>
          </div>
        </div>

        <div class="mt-8 pt-6 border-t border-gray-200">
          <p class="text-xs text-gray-400 flex flex-wrap justify-center">
            <span class="mr-1">This is a user-created website.</span>
            <span class="text-gray-400 text-opacity-70">Powered by <a href="javascript:void(0)" onclick="window.open('https://ftraiseai.onrender.com', '_blank')" class="text-indigo-400 hover:text-indigo-500 transition-colors">FTRAISE AI</a></span>
          </p>
        </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
