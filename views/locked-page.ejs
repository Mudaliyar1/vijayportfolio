<div class="flex flex-col items-center justify-center min-h-[calc(100vh-10rem)] p-4">
  <div class="text-center max-w-2xl mx-auto">
    <!-- Animated Lock Icon -->
    <div class="mb-8 relative">
      <div class="w-32 h-32 mx-auto relative">
        <div class="absolute inset-0 bg-neon-blue/20 rounded-full animate-pulse"></div>
        <div class="absolute inset-0 flex items-center justify-center">
          <i class="fas fa-lock text-6xl text-neon-blue animate-bounce"></i>
        </div>
      </div>
    </div>

    <!-- Lock Message -->
    <h1 class="text-3xl font-bold mb-4 text-white">🚧 This Page is Locked</h1>
    <p class="text-xl text-gray-300 mb-6">We're updating this page right now. Please check back soon.</p>
    
    <!-- Reason -->
    <div class="bg-dark-100 p-4 rounded-lg mb-8 inline-block">
      <p class="text-gray-300">
        <span class="font-semibold">Reason:</span> 
        <%= pageLock.reason %>
      </p>
    </div>

    <!-- Back to Home Button -->
    <a href="/" class="px-6 py-3 bg-gradient-to-r from-neon-blue to-neon-purple text-white rounded-lg hover:opacity-90 transition-all duration-300 inline-flex items-center">
      <i class="fas fa-home mr-2"></i> Return to Home
    </a>
  </div>
</div>

<style>
  @keyframes glow {
    0%, 100% { box-shadow: 0 0 10px 2px rgba(0, 242, 255, 0.5); }
    50% { box-shadow: 0 0 20px 5px rgba(0, 242, 255, 0.8); }
  }

  .animate-glow {
    animation: glow 2s infinite ease-in-out;
  }

  @keyframes bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  .animate-bounce {
    animation: bounce 2s infinite ease-in-out;
  }
</style>
